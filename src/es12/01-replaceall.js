const texto = "Javascript es el mejor lenguaje. Javascript puede vivir en el navegador y en el servidor."
const replacedString = texto.replace('Javascript', 'TypeScript');
const replacedAllString = texto.replaceAll('Javascript', 'TypeScript')

console.log(replacedString);
console.log(replacedAllString);